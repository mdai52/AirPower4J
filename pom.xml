<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>cn.hamm</groupId>
    <artifactId>airpower</artifactId>
    <!-- POM 统一版本-->
    <version>7.1.0</version>
    <name>airpower</name>
    <description>AirPower is a fast backend development tool based on SpringBoot3 and JPA.</description>
    <properties>
        <!-- ==================== 项目版本 ==================== -->
        <airpower.version>7.1.0</airpower.version>
        <airpower-core.version>6.7.0</airpower-core.version>

        <!-- ==================== 构建配置 ==================== -->
        <maven.compiler.encoding>UTF-8</maven.compiler.encoding>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>

        <!-- ==================== 主要框架版本 ==================== -->
        <springboot.version>3.4.4</springboot.version>
        <spring-boot.version>3.4.4</spring-boot.version>
        <spring-framework.version>6.2.5</spring-framework.version>

        <!-- ==================== 第三方库版本 ==================== -->
        <mysql.version>8.0.33</mysql.version>
        <jetbrains.annotations.version>26.0.2</jetbrains.annotations.version>
        <commons.codec.version>1.13</commons.codec.version>
        <lombok.version>1.18.36</lombok.version>
        <relections.version>0.10.1</relections.version>
        <jedis.version>5.1.0</jedis.version>

        <!-- ==================== Spring生态版本 ==================== -->
        <spring.integration.mqtt.version>6.3.3</spring.integration.mqtt.version>
        <springboot.integration.version>3.2.0</springboot.integration.version>
        <spring-data-commons.version>3.4.0</spring-data-commons.version>
        <spring-data-redis.version>${springboot.version}</spring-data-redis.version>
        <spring-context-support.version>${spring-framework.version}</spring-context-support.version>
        <spring-web.version>${spring-framework.version}</spring-web.version>
        <spring-webmvc.version>${spring-framework.version}</spring-webmvc.version>
        <spring-websocket.version>${spring-framework.version}</spring-websocket.version>

        <!-- ==================== Java EE/Jakarta EE版本 ==================== -->
        <jakarta.validation-api.version>3.1.1</jakarta.validation-api.version>
        <jakarta.persistence-api.version>3.2.0</jakarta.persistence-api.version>
        <jakarta.mail.version>2.0.3</jakarta.mail.version>

        <!-- ==================== Spring Boot启动器版本 ==================== -->
        <spring-boot-starter-data-jpa.version>${springboot.version}</spring-boot-starter-data-jpa.version>
        <spring-boot-starter-validation.version>${springboot.version}</spring-boot-starter-validation.version>
        <spring-boot-starter-aop.version>${springboot.version}</spring-boot-starter-aop.version>
        <spring-boot-starter-data-redis.version>${springboot.version}</spring-boot-starter-data-redis.version>

        <!-- ==================== Maven插件版本 ==================== -->
        <maven.source.plugin.version>3.4.0</maven.source.plugin.version>
        <maven.central.publishing.plugin.version>0.9.0</maven.central.publishing.plugin.version>
        <maven.javadoc.plugin.version>3.12.0</maven.javadoc.plugin.version>
        <maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
    </properties>
    <packaging>pom</packaging>
    <modules>
        <module>airpower-ai</module>
        <module>airpower-api</module>
        <module>airpower-exception</module>
        <module>airpower-curd</module>
        <module>airpower-email</module>
        <module>airpower-cookie</module>
        <module>airpower-mqtt</module>
        <module>airpower-open</module>
        <module>airpower-redis</module>
        <module>airpower-websocket</module>
        <module>airpower-file</module>
    </modules>
    <dependencyManagement>
        <dependencies>
            <!-- ==================== AirPower 内部依赖 ==================== -->
            <dependency>
                <groupId>cn.hamm</groupId>
                <artifactId>airpower-web</artifactId>
                <version>${airpower.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.hamm</groupId>
                <artifactId>airpower-core</artifactId>
                <version>${airpower-core.version}</version>
            </dependency>

            <!-- ==================== Spring Boot 依赖 ==================== -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${springboot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-test</artifactId>
                <version>${springboot.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-integration</artifactId>
                <version>${springboot.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-thymeleaf</artifactId>
                <version>${springboot.version}</version>
            </dependency>

            <!-- ==================== 数据库相关依赖 ==================== -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <scope>runtime</scope>
                <version>${mysql.version}</version>
            </dependency>

            <!-- ==================== Spring 生态依赖 ==================== -->
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-mqtt</artifactId>
                <version>${spring.integration.mqtt.version}</version>
            </dependency>

            <!-- ==================== Java EE/Jakarta EE 依赖 ==================== -->
            <dependency>
                <groupId>jakarta.validation</groupId>
                <artifactId>jakarta.validation-api</artifactId>
                <version>${jakarta.validation-api.version}</version>
            </dependency>

            <!-- ==================== 第三方通用库 ==================== -->
            <dependency>
                <groupId>org.jetbrains</groupId>
                <artifactId>annotations</artifactId>
                <scope>compile</scope>
                <version>${jetbrains.annotations.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-codec</groupId>
                <artifactId>commons-codec</artifactId>
                <version>${commons.codec.version}</version>
            </dependency>
            <dependency>
                <groupId>org.reflections</groupId>
                <artifactId>reflections</artifactId>
                <version>${relections.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- ==================== 发布插件 ==================== -->
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>${maven.central.publishing.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>true</autoPublish>
                </configuration>
            </plugin>

            <!-- ==================== 源码插件 ==================== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven.source.plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- ==================== 文档插件 ==================== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven.javadoc.plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <doclint>none</doclint>
                </configuration>
            </plugin>

            <!-- ==================== 签名插件 ==================== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <version>${maven.gpg.plugin.version}</version>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                        <configuration>
                            <keyname>5FE4833A28EDA3991EA4F1D239A8BA7AF33610A8</keyname>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <licenses>
        <license>
            <name>The MIT License</name>
            <url>https://raw.githubusercontent.com/AirPowerTeam/AirPower4J/main/LICENSE</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <url>https://github.com/AirPowerTeam/AirPower4J</url>
    <scm>
        <connection>scm:git:git://github.com/AirPowerTeam/AirPower4J.git</connection>
        <developerConnection>scm:git:git://github.com/AirPowerTeam/AirPower4J.git</developerConnection>
        <url>https://github.com/AirPowerTeam/AirPower4J</url>
        <tag>HEAD</tag>
    </scm>
    <developers>
        <developer>
            <id>HammCn</id>
            <name>Hamm</name>
            <email>admin@hamm.cn</email>
            <roles>
                <role>developer</role>
            </roles>
            <timezone>+8</timezone>
            <properties>
                <picUrl>https://avatars.githubusercontent.com/u/16929327</picUrl>
            </properties>
        </developer>
    </developers>
</project>
